# build artifacts
dino_metal
dino_grab
dino_shot
dino_yuv
*.o
*.dSYM/

# release bundle / zip (produced by `make dist`; uploaded to GitHub Releases)
DinoLiteMac.app/
DinoLiteMac-v*-macOS-arm64.zip

# captures / scratch
*.png
*.ppm
*.pgm
*.bin
.DS_Store

# Linux kernel reference sources (GPL-2.0) - see README for the upstream URL.
# Not redistributed here; keep locally for reference if you have them.
sn9c20x.c
gspca.h

# Proprietary vendor material - reference only, never committed.
dinocapture_recovered/
*.exe
*.exe.c
